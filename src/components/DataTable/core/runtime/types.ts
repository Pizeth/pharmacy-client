/**
 * Default runtime maps.
 *
 * These are intentionally empty.
 *
 * Feature modules extend them later.
 */
export interface DataTableRuntimeMaps {
  commands: {};

  services: {};

  plugins: {};

  events: {};
}
