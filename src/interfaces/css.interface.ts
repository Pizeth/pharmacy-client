export interface StarfieldOptions {
  count?: number; // number of static stars
  colors?: string[]; // palette of star colors
  twinkle?: boolean; // enable twinkling animation
  shootingStars?: number; // number of shooting stars
}
